class Line(object):
    def __init__(self, x_id, y_id, data_frame, color, width):
        self.x_id = x_id
        self.y_id = y_id
        self.data_frame = data_frame
        self.color = color
        self.width = width
